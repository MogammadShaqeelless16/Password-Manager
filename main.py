import customtkinter as ctk
import json
from card_manager import CardManager

# Create a CardManager instance to manage the application logic
app = CardManager()

# Start the event loop and run the application
app.mainloop()
